static String removeSuffix(final String input, final String suffix) {
    if (input.endsWith(suffix)) {
        return input.substring(0, input.length() - suffix.length())
    }

    input
}

return removeSuffix(_1, _2)